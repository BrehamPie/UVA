#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;i++)
int main()
{
    int t;cin>>t;
    while(t--)
    {
    int n;cin>>n;int arr[n];rep(i,n) cin>>arr[i];sort(arr,arr+n);
    cout<<2*(arr[n-1]-arr[0])<<endl;
    }
    return 0;
}
