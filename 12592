#include<bits/stdc++.h>
#define rep(i,n) for(int i=0;i<n;i++)
#define per(i,n) for(int i=n-1;i>=0;i--)

using namespace std;

int main()
{
    map<string,string> mmm;
    int n;cin>>n;getchar();
    while(n--)
    {

        string x,y;
        getline(cin,x);
        getline(cin,y);
        mmm[x]=y;
    }
    int d;cin>>d;getchar();
    while(d--)
    {

        string p;
        getline(cin,p);
        cout<<mmm[p]<<endl;
    }
    return 0;
}
