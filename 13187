#include <stdio.h>
int main()
{

    int n,i;
    scanf("%d", &n);
     for(i=0;i<n;i++)
    {
        long long int p;
        scanf("%lld", &p);
        printf("%lld\n",((p+1)*(p+1) - 1));
    }
   return 0;
}
