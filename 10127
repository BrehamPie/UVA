#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    while(scanf("%lld",&n)==1)
    {
        int d=1,cnt=1;
        while(d%n)
        {
            cnt++;
            d=(d*10+1)%n;
        }
        cout<<cnt<<endl;
    }
}
